# ClickHouse
# Replace with your ClickHouse HTTP(S) endpoint. Example using the provided host:
CLICKHOUSE_URL=https://v0tapvvnar.us-east-2.aws.clickhouse.cloud:8443
CLICKHOUSE_USER=default
# Do NOT commit production passwords to the repository. Set the password in GitHub Actions
# secrets or a local .env file that is kept out of source control.
# For local testing you can export the password in your shell (example):
#   export CLICKHOUSE_PASSWORD="<your_password>"  # do not commit
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=default

# S3 / AWS
S3_BUCKET=your-bucket-name
S3_KEY_PREFIX=optional/prefix
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Optional override for export date (YYYY-MM-DD). If omitted, yesterday (UTC) is used.
EXPORT_DATE=
